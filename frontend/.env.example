NEXT_PUBLIC_API_URL=http://localhost:5000/api/v1

# Optional — surfaces the support contact on the settings page
# (settings/page.tsx). Leave empty to hide the direct-contact line;
# the self-service guidance block always renders.
NEXT_PUBLIC_SUPPORT_EMAIL=
